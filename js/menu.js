function menu(){
	// Reiniciar el nivel
	if(presionando[enter]){

		// Reiniciando personaje
	    personajex = 0;
		personajey = (hcanvas - hPersonaje);
		saltando = true;
        siguesaltando = false;

        // Reiniciando puntaje
	    puntuacion = 0;

        // Reiniciando orbes
        // Ciclo for para leer los distintos orbes
        
	}
}

// Función para ejecutar el juego
function iniciarJuego(){
    document.querySelector('#info').style.display = 'none';
}